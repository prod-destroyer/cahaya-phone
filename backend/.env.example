# Database settings (copy values from your setup)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=cahaya_phone_crm

# JWT secret
JWT_SECRET=supersecret

# Mail settings (optional) - if not set, server will return token in response for development
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USER=you@example.com
MAIL_PASS=supersecret
MAIL_FROM=Cahaya <no-reply@example.com>

# Frontend URL used in reset link (optional)
FRONTEND_URL=http://localhost:5500
